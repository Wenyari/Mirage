# AIGC 平台管理员后台实现计划

## 项目概述

实现一个完整的管理员后台系统，包含 6 个主要页面和配套基础设施。

**技术栈**：
- React 19 + TypeScript + Vite 7
- shadcn/ui (Radix UI + Tailwind CSS)
- TanStack Query v5 (数据管理)
- React Hook Form + Zod (表单)
- React Router v6 (路由)
- MSW (Mock 数据)
- Recharts (图表)
- TanStack Table (表格)
- axios (HTTP 客户端)
- Zustand (状态管理)

**后端状态**：使用 MSW 提供 Mock 数据，所有接口前缀 `/api/admin`

**路由前缀**：`/wadminw`

**认证策略**：实现完整登录页面和 Token 管理，但暂时不启用，直接进入管理主页

---

## 实现阶段

### 阶段 0：基础设施搭建

**安装依赖**：
```bash
# 生产依赖
npm install react-router-dom@^6.28.0 @tanstack/react-query@^5.62.12 @tanstack/react-query-devtools@^5.62.12 @tanstack/react-table@^8.20.5 react-hook-form@^7.54.2 zod@^3.24.1 @hookform/resolvers@^3.9.1 axios@^1.7.9 recharts@^2.15.0 date-fns@^4.1.0 sonner@^1.7.3 zustand@^5.0.2 xlsx@^0.18.5

# 开发依赖
npm install -D msw@^2.7.0
```

**安装 shadcn/ui 组件**（约 30 个）：
```bash
# 基础组件
npx shadcn@latest add button card separator scroll-area skeleton

# 导航组件
npx shadcn@latest add navigation-menu breadcrumb dropdown-menu

# 表单组件
npx shadcn@latest add form input label select textarea switch radio-group slider checkbox

# 反馈组件
npx shadcn@latest add dialog alert badge toast alert-dialog progress

# 数据展示
npx shadcn@latest add table tabs avatar tooltip popover chart
```

**创建目录结构**：
```
src/
├── lib/
│   └── api.ts                        # Axios 实例 + 拦截器
├── types/
│   ├── index.ts, admin.ts, user.ts, cdk.ts, key.ts, model.ts, api.ts
├── services/admin/
│   ├── dashboard.ts, users.ts, cdk.ts, keys.ts, models.ts, playground.ts
├── mocks/
│   ├── browser.ts                    # MSW 配置
│   ├── handlers/
│   │   └── index.ts, dashboard.ts, users.ts, cdk.ts, keys.ts, models.ts, playground.ts
│   └── data/
│       └── users.ts, cdk.ts, keys.ts, models.ts, stats.ts
├── components/
│   ├── ui/                           # shadcn 组件（自动生成）
│   ├── admin/                        # 业务组件
│   ├── charts/                       # 图表组件
│   ├── tables/                       # 表格组件
│   └── guards/
│       └── AdminGuard.tsx
├── layouts/
│   └── AdminLayout.tsx               # 侧边栏布局
├── pages/admin/
│   ├── Dashboard.tsx, UserManager.tsx, CDKManager.tsx
│   ├── KeyPool.tsx, ModelManager.tsx, Playground.tsx
├── hooks/
│   ├── useUsers.ts, useCDK.ts, useKeys.ts, useModels.ts
├── store/
│   └── authStore.ts                  # Zustand 状态
├── config/
│   ├── routes.tsx                    # 路由配置
│   └── queryClient.ts                # TanStack Query 配置
└── utils/
    ├── format.ts, validation.ts, export.ts
```

**配置核心文件**：
1. `src/lib/api.ts` - axios 实例，拦截器处理 JWT 和 401
2. `src/config/queryClient.ts` - TanStack Query 配置
3. `src/mocks/browser.ts` - MSW 启动配置
4. `src/main.tsx` - 启动 MSW（仅开发环境）

---

### 阶段 1：认证系统与布局

**目标**：建立应用骨架

**实现步骤**：
1. 创建 `src/store/authStore.ts` - Zustand 状态管理（token, user, login, logout）
2. 创建 `src/components/guards/AdminGuard.tsx` - 权限守卫（暂时直接放行）
3. 创建 `src/layouts/AdminLayout.tsx` - 主布局（Sidebar + Header + Outlet）
4. 创建 `src/components/admin/AdminSidebar.tsx` - 左侧导航（6 个链接）
5. 创建 `src/components/admin/AdminHeader.tsx` - 顶部栏（面包屑 + 主题切换）
6. 创建 `src/config/routes.tsx` - 路由配置
7. 修改 `src/App.tsx` - 使用 RouterProvider
8. 创建占位页面（6 个空白页面，仅显示标题）

**完成标志**：访问 `http://localhost:5173/wadminw/dashboard` 看到布局框架，导航正常

---

### 阶段 2：Dashboard 仪表盘

**Mock 数据**：
- `src/mocks/data/stats.ts` - 统计数据（overview + 7/30天趋势）
- `src/mocks/handlers/dashboard.ts` - GET /api/admin/stats/overview, /chart

**实现步骤**：
1. 实现 `src/services/admin/dashboard.ts` - API 调用函数
2. 实现 `src/hooks/useDashboardStats.ts` - useQuery Hook
3. 创建 4 个统计卡片（Card 组件）
4. 创建 `src/components/charts/UserGrowthChart.tsx` - Recharts LineChart
5. 创建 `src/components/charts/TokenUsageChart.tsx` - Recharts BarChart
6. 组装 `src/pages/admin/Dashboard.tsx`
7. 添加 Loading Skeleton 和错误处理

**关键技术**：
- TanStack Query 的 `useQuery`
- Recharts 配置（响应式、主题颜色）
- Grid 布局

---

### 阶段 3：用户管理页面（最复杂）

**Mock 数据**：
- `src/mocks/data/users.ts` - 50+ 条用户数据
- `src/mocks/handlers/users.ts` - GET /users, PATCH /users/:id/balance, PATCH /users/:id/profile

**实现步骤**：
1. 定义 `src/types/user.ts` - User, UserLevel, UserStatus 类型
2. 实现 `src/services/admin/users.ts` - getUsers, updateBalance, updateProfile
3. 实现 `src/hooks/useUsers.ts` - useUsers, useUpdateBalance, useUpdateProfile
4. 创建 `src/components/admin/LevelBadge.tsx` - T1-T5 徽章
5. 创建 `src/components/admin/StatusBadge.tsx` - 状态徽章
6. 创建 `src/components/tables/UsersTable.tsx` - TanStack Table
   - 列：ID, Avatar+Email, Level, Balance, Status (Switch), Actions
   - 分页、搜索、筛选
7. 创建 `src/components/admin/UserEditDialog.tsx` - 编辑对话框（React Hook Form + Zod）
8. 创建 `src/components/admin/UserBalanceDialog.tsx` - 充值对话框
9. 组装 `src/pages/admin/UserManager.tsx`

**关键技术**：
- TanStack Table 列定义
- React Hook Form + Zod 验证
- useMutation + invalidateQueries
- 乐观更新

---

### 阶段 4：CDK 管理页面

**Mock 数据**：
- `src/mocks/data/cdk.ts` - CDK 列表数据
- `src/mocks/handlers/cdk.ts` - POST /cdk/generate, GET /cdk, POST /cdk/void

**实现步骤**：
1. 定义 `src/types/cdk.ts` - CDK, CDKType 类型
2. 实现 `src/services/admin/cdk.ts` - generateCDK, getCDKList, voidCDK
3. 实现 `src/hooks/useCDK.ts`
4. 创建 `src/components/admin/CDKGenerateForm.tsx` - 生成表单（Card + Form）
5. 创建 `src/components/tables/CDKTable.tsx` - CDK 列表表格
6. 实现 `src/utils/export.ts` - Excel 导出（使用 xlsx）
7. 组装 `src/pages/admin/CDKManager.tsx`

**关键技术**：
- CDK 部分隐藏显示（前 6 位 + ****）
- 生成后自动下载 Excel
- 批量作废功能

---

### 阶段 5：密钥池管理页面

**Mock 数据**：
- `src/mocks/data/keys.ts` - 密钥列表
- `src/mocks/handlers/keys.ts` - GET /keys, POST /keys, DELETE /keys/:id, POST /keys/check

**实现步骤**：
1. 定义 `src/types/key.ts` - Key, KeyStatus 类型
2. 实现 `src/services/admin/keys.ts`
3. 实现 `src/hooks/useKeys.ts`
4. 创建 `src/components/admin/KeyAddDialog.tsx` - 批量添加对话框（Textarea 多行）
5. 创建 `src/components/tables/KeysTable.tsx`
6. 组装 `src/pages/admin/KeyPool.tsx`
7. 实现健康检测按钮和 Loading 状态

**关键技术**：
- 密钥隐藏显示（sk-****）
- 批量添加解析
- 异步健康检测

---

### 阶段 6：模型与等级配置页面

**Mock 数据**：
- `src/mocks/data/models.ts` - 模型列表 + T1-T5 配置
- `src/mocks/handlers/models.ts` - GET /models, PATCH /models/:id, GET/PUT /config/membership

**实现步骤**：
1. 定义 `src/types/model.ts` - Model, MembershipConfig 类型
2. 实现 `src/services/admin/models.ts`
3. 实现 `src/hooks/useModels.ts`
4. 创建 Tabs 布局
5. **Tab 1**：模型列表表格（行内编辑 + Switch）
6. **Tab 2**：5 个 Card 展示 T1-T5（Slider + Input）
7. 组装 `src/pages/admin/ModelManager.tsx`

**关键技术**：
- 表格行内编辑状态管理
- Slider 与 Input 双向绑定
- 批量保存配置

---

### 阶段 7：测试沙箱页面

**Mock 数据**：
- `src/mocks/handlers/playground.ts` - POST /test/generate

**实现步骤**：
1. 实现 `src/services/admin/playground.ts`
2. 创建两栏布局
   - 左侧：Model Select + Prompt Textarea + JSON Input
   - 右侧：Tabs（Preview + Debug JSON）
3. 组装 `src/pages/admin/Playground.tsx`
4. 实现提交逻辑和 Loading 状态
5. 实现 Debug JSON pretty-print

**关键技术**：
- JSON 编辑和格式化
- 图片/视频预览
- 错误边界

---

### 阶段 8：优化与完善

**优化项**：
1. 添加全局 Toast（sonner）
2. 添加错误边界组件
3. 优化 Loading Skeleton
4. 空状态处理
5. 代码 lint 和格式化
6. 响应式适配（移动端 Sidebar）
7. 基础无障碍优化

---

## 关键文件清单

### 核心配置文件（5 个最关键）
1. **F:\Mirage\frontend\src\lib\api.ts** - Axios 配置核心
2. **F:\Mirage\frontend\src\mocks\browser.ts** - MSW 启动配置
3. **F:\Mirage\frontend\src\layouts\AdminLayout.tsx** - 布局核心
4. **F:\Mirage\frontend\src\config\routes.tsx** - 路由配置核心
5. **F:\Mirage\frontend\src\pages\admin\UserManager.tsx** - 最复杂页面，其他页面的实现模板

### Mock 数据文件
- `F:\Mirage\frontend\src\mocks\handlers\index.ts` - 总入口
- `F:\Mirage\frontend\src\mocks\data\*.ts` - 各模块 Mock 数据

### 类型定义文件
- `F:\Mirage\frontend\src\types\admin.ts` - 管理员类型核心
- `F:\Mirage\frontend\src\types\user.ts` - 用户类型
- `F:\Mirage\frontend\src\types\cdk.ts, key.ts, model.ts` - 各模块类型

### 服务层文件
- `F:\Mirage\frontend\src\services\admin\*.ts` - 所有 API 调用函数

### 组件文件
- `F:\Mirage\frontend\src\components\admin\*.tsx` - 业务组件
- `F:\Mirage\frontend\src\components\charts\*.tsx` - 图表组件
- `F:\Mirage\frontend\src\components\tables\*.tsx` - 表格组件

### 页面文件
- `F:\Mirage\frontend\src\pages\admin\*.tsx` - 6 个管理页面

---

## 技术要点

### 1. Axios 配置
```typescript
// src/lib/api.ts
import axios from 'axios';

const api = axios.create({
  baseURL: '/api',
  timeout: 10000,
});

// 请求拦截器：添加 JWT Token
api.interceptors.request.use(config => {
  const token = localStorage.getItem('token');
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

// 响应拦截器：处理 401
api.interceptors.response.use(
  response => response.data,
  error => {
    if (error.response?.status === 401) {
      // 跳转登录
    }
    return Promise.reject(error);
  }
);
```

### 2. MSW 启动
```typescript
// src/main.tsx
import { worker } from './mocks/browser';

if (import.meta.env.DEV) {
  worker.start({ onUnhandledRequest: 'bypass' });
}
```

### 3. 路由配置
```typescript
// src/config/routes.tsx
export const router = createBrowserRouter([
  {
    path: '/wadminw',
    element: <AdminLayout />,
    children: [
      { index: true, element: <Navigate to="/wadminw/dashboard" /> },
      { path: 'dashboard', element: <Dashboard /> },
      { path: 'users', element: <UserManager /> },
      // ... 其他路由
    ],
  },
]);
```

### 4. TanStack Query Hook 示例
```typescript
// src/hooks/useUsers.ts
export function useUsers(params: UserListParams) {
  return useQuery({
    queryKey: ['admin', 'users', params],
    queryFn: () => getUsers(params),
  });
}

export function useUpdateBalance() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ userId, amount, reason }) => updateUserBalance(userId, amount, reason),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['admin', 'users'] });
    },
  });
}
```

### 5. Form 验证示例
```typescript
// React Hook Form + Zod
const formSchema = z.object({
  amount: z.number().min(1),
  reason: z.string().min(1),
});

const form = useForm<z.infer<typeof formSchema>>({
  resolver: zodResolver(formSchema),
});
```

---

## 后端对接切换方案

**当后端 API 就绪时**：
1. 删除 `src/mocks/` 目录
2. 删除 `src/main.tsx` 中的 MSW 启动代码
3. 卸载 `msw` 依赖
4. 修改 `src/lib/api.ts` 的 `baseURL` 为真实后端地址
5. 验证类型定义与后端响应一致
6. 测试所有功能

---

## 预计工作量

- 阶段 0：2-3 小时
- 阶段 1：3-4 小时
- 阶段 2：4-5 小时
- 阶段 3：6-8 小时
- 阶段 4：4-5 小时
- 阶段 5：3-4 小时
- 阶段 6：4-5 小时
- 阶段 7：3-4 小时
- 阶段 8：2-3 小时

**总计**：约 31-41 小时（分 8 个阶段逐步实现）

---

## 实现顺序建议

推荐顺序：0 → 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8

每个阶段完成后检查：
- 功能正常运行
- 无 TypeScript 错误
- 无 ESLint 警告
- UI 响应式正常
- Loading/Error 状态正常
