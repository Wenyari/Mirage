# 活动积分系统实施计划

## 重要声明

**本系统尚未上线，不需要考虑向后兼容问题。** 所有设计以清晰简洁为原则，删除冗余字段和逻辑。

---

## 一、需求概述

实现一个完整的活动积分系统，包含以下核心功能：

### 1.1 积分隔离机制
- 将用户的单一积分余额拆分为"充值积分"和"活动积分"
- 消费时优先扣除活动积分，不足时扣除充值积分
- **删除旧的 balance 字段**，只保留 recharge_balance 和 activity_balance

### 1.2 每日签到功能
- 用户每天可签到一次获得活动积分
- 支持连续签到奖励（1-7天循环）
- 签到奖励可由管理员配置

### 1.3 活动积分模块
- 管理员可创建活动（配置积分数量、有效期、领取限制等）
- 用户访问活动接口自动领取积分
- 活动积分有时效性，到期自动扣除

### 1.4 完整流水记录
- 所有积分变动（包括活动积分）都记录在 transactions 表
- 区分充值积分流水和活动积分流水

### 1.5 代码重构：统一扣费/退款逻辑 ⚠️

**当前问题：**
扣费和退款逻辑分散在3处，代码重复且无法支持活动积分：

1. **task_service.py line 115**：直接 `user.balance -= Decimal(str(cost))`
2. **task_service.py line 206**：直接 `user.balance += task.cost_points`
3. **worker.py line 370**：原始 SQL `u.balance = u.balance + :refund_amount`

**重构目标：**
- **删除** task_service.py 中的扣费和退款代码
- **删除** worker.py 中的退款代码
- **统一** 使用 pay_service.py 中的 check_and_deduct_balance() 和 execute_refund()
- **保留** worker.py 中的 _should_refund_on_failure() 判断逻辑（内容违规不退款）

**技术挑战：**
- worker.py 使用原始 SQL 避免 session 缓存问题
- 解决方案：在 worker 中调用 pay_service 后执行 `db.session.remove()` 清理 session

---

## 二、数据库设计变更

### 2.1 修改 users 表

**删除字段：**
```sql
-- 删除旧字段（不需要向后兼容）
DROP COLUMN balance
```

**新增字段：**
```sql
recharge_balance DECIMAL(10,2) DEFAULT 0.00 NOT NULL COMMENT '充值积分'
activity_balance DECIMAL(10,2) DEFAULT 0.00 NOT NULL COMMENT '活动积分'
last_checkin_at DATETIME NULL COMMENT '最后签到时间'
total_checkin_days INT DEFAULT 0 NOT NULL COMMENT '累计签到天数'
```

**注意：** 由于数据库为空，直接修改模型定义即可，无需迁移脚本。

### 2.2 修改 transactions 表

**扩展交易类型枚举：**
```sql
-- 原有类型：'recharge', 'task_cost', 'refund', 'system'
-- 新增类型：'checkin', 'activity_grant', 'activity_expire'
type ENUM('recharge', 'task_cost', 'refund', 'system',
          'checkin', 'activity_grant', 'activity_expire')
```

**新增字段：**
```sql
balance_type ENUM('recharge', 'activity') NULL COMMENT '积分类型：充值/活动'
activity_id INT NULL COMMENT '关联活动ID（如果是活动相关流水）'
```

**新增索引：**
```sql
INDEX idx_user_type_balance (user_id, type, balance_type)
```

### 2.3 新增 activities 表（活动配置）

```sql
CREATE TABLE activities (
    id INT AUTO_INCREMENT PRIMARY KEY,
    code VARCHAR(50) UNIQUE NOT NULL COMMENT '活动代码（用于领取）',
    name VARCHAR(100) NOT NULL COMMENT '活动名称',
    description TEXT NULL COMMENT '活动描述',
    points DECIMAL(10,2) NOT NULL COMMENT '赠送积分',
    expire_days INT NULL COMMENT '积分有效期(天)，NULL表示永久',
    max_claims_per_user INT DEFAULT 1 NOT NULL COMMENT '每用户最多领取次数',
    required_level SMALLINT NULL COMMENT '最低会员等级要求',
    start_at DATETIME NULL COMMENT '活动开始时间',
    end_at DATETIME NULL COMMENT '活动结束时间',
    status ENUM('active', 'paused', 'ended') DEFAULT 'active' NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    INDEX idx_status_time (status, start_at, end_at),
    INDEX idx_code (code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 2.4 新增 activity_claims 表（活动领取记录）

```sql
CREATE TABLE activity_claims (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    activity_id INT NOT NULL,
    points_granted DECIMAL(10,2) NOT NULL COMMENT '实际赠送积分',
    expire_at DATETIME NULL COMMENT '积分过期时间',
    status ENUM('active', 'expired', 'used') DEFAULT 'active' NOT NULL,
    claimed_at DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL,
    expired_at DATETIME NULL COMMENT '实际过期扣除时间',

    INDEX idx_user_activity (user_id, activity_id),
    INDEX idx_expire_status (expire_at, status),
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (activity_id) REFERENCES activities(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 2.5 新增 checkin_configs 表（签到配置）

```sql
CREATE TABLE checkin_configs (
    id INT AUTO_INCREMENT PRIMARY KEY,
    day INT UNIQUE NOT NULL COMMENT '连续签到天数(1-7)',
    points DECIMAL(10,2) NOT NULL COMMENT '奖励积分',
    is_active TINYINT DEFAULT 1 NOT NULL COMMENT '是否启用',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**初始化数据：**
```sql
INSERT INTO checkin_configs (day, points) VALUES
(1, 10), (2, 15), (3, 20), (4, 25),
(5, 30), (6, 40), (7, 50);
```

---

## 三、业务逻辑实现

### 3.1 重构 pay_service.py - 优先扣除逻辑

**修改函数：** `check_and_deduct_balance(user_id, amount, task_id)`

**新逻辑：**
```python
def check_and_deduct_balance(user_id: int, amount: float, task_id: str):
    """
    检查余额并扣除（优先扣除活动积分）

    扣除策略：
    1. 优先扣除 activity_balance
    2. 不足时扣除 recharge_balance
    3. 两者合计不足时抛出异常

    流水记录：
    - 如果只扣除活动积分：1条流水（balance_type='activity'）
    - 如果同时扣除：2条流水（分别记录）
    """
    user = User.query.with_for_update().get(user_id)  # 悲观锁
    if not user:
        raise ValueError("User not found")

    # 计算总余额
    total_balance = user.recharge_balance + user.activity_balance
    if total_balance < amount:
        raise ValueError(f"Insufficient balance. Required: {amount}, Available: {total_balance}")

    # 计算扣除方案
    deduct_from_activity = min(user.activity_balance, amount)
    deduct_from_recharge = amount - deduct_from_activity

    # 扣除积分
    user.activity_balance -= deduct_from_activity
    user.recharge_balance -= deduct_from_recharge

    # 记录流水（活动积分部分）
    if deduct_from_activity > 0:
        transaction1 = Transaction(
            user_id=user_id,
            type='task_cost',
            balance_type='activity',
            amount=-deduct_from_activity,
            balance_snapshot=user.activity_balance,
            related_id=task_id,
            remark=f"Task cost (activity): {task_id}"
        )
        db.session.add(transaction1)

    # 记录流水（充值积分部分）
    if deduct_from_recharge > 0:
        transaction2 = Transaction(
            user_id=user_id,
            type='task_cost',
            balance_type='recharge',
            amount=-deduct_from_recharge,
            balance_snapshot=user.recharge_balance,
            related_id=task_id,
            remark=f"Task cost (recharge): {task_id}"
        )
        db.session.add(transaction2)

    db.session.commit()
```

**修改函数：** `redeem_cdk(user_id, code)`

**变更：**
```python
# 原来：user.balance += points
# 改为：user.recharge_balance += points

# Transaction 记录添加：balance_type='recharge'
transaction = Transaction(
    user_id=user_id,
    type='recharge',
    balance_type='recharge',  # 新增
    amount=points,
    balance_snapshot=user.recharge_balance,  # 改为 recharge_balance
    related_id=str(cdk.id),
    remark=f"CDK recharge: {code}"
)
```

**修改函数：** `execute_refund(user_id, amount, task_id, reason)`

**变更：**
```python
# 简化处理：统一退到 recharge_balance
user.recharge_balance += amount

transaction = Transaction(
    user_id=user_id,
    type='refund',
    balance_type='recharge',  # 新增
    amount=amount,
    balance_snapshot=user.recharge_balance,  # 改为 recharge_balance
    related_id=task_id,
    remark=f"Refund: {reason}"
)
```

### 3.2 重构 task_service.py - 使用统一接口

**修改 submit_task() 方法：**

```python
# 删除原有代码（line 97-98, 115）：
# if user.balance < cost:
#     raise ValueError(...)
# user.balance -= Decimal(str(cost))

# 改为调用 pay_service：
from app.services.pay_service import check_and_deduct_balance

# 在创建任务前扣费
try:
    check_and_deduct_balance(user_id, cost, task_id)
except ValueError as e:
    raise ValueError(str(e))
```

**修改 cancel_task() 方法：**

```python
# 删除原有代码（line 205-206）：
# user = User.query.get(user_id)
# user.balance += task.cost_points

# 改为调用 pay_service：
from app.services.pay_service import execute_refund

execute_refund(user_id, task.cost_points, task_id, "User cancelled")
```

### 3.3 重构 worker.py - 使用统一接口

**修改任务失败处理逻辑：**

```python
# 删除原有代码（line 363-379）：
# fail_with_refund_sql = text(...)
# u.balance = u.balance + :refund_amount

# 改为：
from app.services.pay_service import execute_refund

if should_refund:
    # 查询 cost_points
    cost_sql = text("SELECT cost_points, user_id FROM tasks WHERE id = :task_id")
    cost_result = query_sql(cost_sql, {"task_id": task_id})

    if cost_result:
        refund_amount, user_id = cost_result

        # 更新任务状态
        fail_sql = text("""
            UPDATE tasks
            SET status = 'failed',
                progress = 0,
                fail_reason = :fail_reason,
                finished_at = :finished_at
            WHERE id = :task_id
        """)
        execute_sql(fail_sql, {
            "task_id": task_id,
            "fail_reason": fail_reason,
            "finished_at": datetime.now(ZoneInfo("Asia/Shanghai"))
        })

        # 使用 pay_service 退款
        with app.app_context():
            execute_refund(user_id, refund_amount, task_id, fail_reason)
            db.session.remove()  # 清理 session 避免缓存问题

        logger.info(f"Refunded {refund_amount} points to user {user_id}")
```

### 3.4 新建 activity_service.py - 活动核心逻辑

**位置：** `F:\Mirage\backend\app\services\activity_service.py`

**核心功能：**

#### 1. claim_activity(user_id, activity_code)
```python
def claim_activity(user_id: int, activity_code: str) -> dict:
    """
    领取活动积分

    业务流程：
    1. 验证活动（存在、有效期、状态）
    2. 验证用户权限（等级要求）
    3. 检查领取次数限制
    4. 使用悲观锁更新 activity_balance
    5. 创建 activity_claims 记录
    6. 创建 transactions 流水
    """
    # 查询活动
    activity = Activity.query.filter_by(code=activity_code).with_for_update().first()
    if not activity:
        raise ValueError("Activity not found")

    # 验证活动状态
    if activity.status != 'active':
        raise ValueError(f"Activity is {activity.status}")

    # 验证时间范围
    now = datetime.now(ZoneInfo("Asia/Shanghai"))
    if activity.start_at and activity.start_at > now:
        raise ValueError("Activity has not started")
    if activity.end_at and activity.end_at < now:
        raise ValueError("Activity has ended")

    # 验证用户等级
    user = User.query.with_for_update().get(user_id)
    if activity.required_level and user.level < activity.required_level:
        raise ValueError(f"Requires level T{activity.required_level} or higher")

    # 检查领取次数
    claim_count = ActivityClaim.query.filter_by(
        user_id=user_id,
        activity_id=activity.id
    ).count()

    if claim_count >= activity.max_claims_per_user:
        raise ValueError("Claim limit reached")

    # 增加活动积分
    user.activity_balance += activity.points

    # 计算过期时间
    expire_at = None
    if activity.expire_days:
        expire_at = datetime.now(ZoneInfo("Asia/Shanghai")) + timedelta(days=activity.expire_days)

    # 创建领取记录
    claim = ActivityClaim(
        user_id=user_id,
        activity_id=activity.id,
        points_granted=activity.points,
        expire_at=expire_at,
        status='active'
    )
    db.session.add(claim)

    # 创建流水
    transaction = Transaction(
        user_id=user_id,
        type='activity_grant',
        balance_type='activity',
        amount=activity.points,
        balance_snapshot=user.activity_balance,
        activity_id=activity.id,
        remark=f"Activity: {activity.name}"
    )
    db.session.add(transaction)

    db.session.commit()

    return {
        "points": float(activity.points),
        "expire_at": expire_at.isoformat() if expire_at else None,
        "current_activity_balance": float(user.activity_balance),
        "total_balance": float(user.recharge_balance + user.activity_balance)
    }
```

#### 2. daily_checkin(user_id)
```python
def daily_checkin(user_id: int) -> dict:
    """
    每日签到

    业务流程：
    1. 检查今天是否已签到
    2. 判断连续签到天数
    3. 获取奖励积分
    4. 更新 activity_balance
    5. 更新签到信息
    6. 创建流水
    """
    user = User.query.with_for_update().get(user_id)
    if not user:
        raise ValueError("User not found")

    now = datetime.now(ZoneInfo("Asia/Shanghai"))
    today_date = now.strftime('%Y-%m-%d')

    # 检查今天是否已签到
    if user.last_checkin_at:
        last_date = user.last_checkin_at.strftime('%Y-%m-%d')
        if last_date == today_date:
            raise ValueError("Already checked in today")

        # 判断是否连续签到
        yesterday = (now - timedelta(days=1)).strftime('%Y-%m-%d')
        if last_date == yesterday:
            # 连续签到，天数+1（循环1-7）
            consecutive_days = (user.total_checkin_days % 7) + 1
        else:
            # 断签，重置为第1天
            consecutive_days = 1
    else:
        consecutive_days = 1

    # 从配置获取奖励积分
    checkin_config = CheckinConfig.query.filter_by(
        day=consecutive_days,
        is_active=1
    ).first()

    if not checkin_config:
        raise ValueError("Checkin config not found")

    points = checkin_config.points

    # 更新用户数据
    user.activity_balance += points
    user.last_checkin_at = now
    user.total_checkin_days += 1

    # 创建流水
    transaction = Transaction(
        user_id=user_id,
        type='checkin',
        balance_type='activity',
        amount=points,
        balance_snapshot=user.activity_balance,
        remark=f"Daily checkin (day {consecutive_days})"
    )
    db.session.add(transaction)

    db.session.commit()

    return {
        "points": float(points),
        "consecutive_days": consecutive_days,
        "total_checkin_days": user.total_checkin_days,
        "current_activity_balance": float(user.activity_balance),
        "total_balance": float(user.recharge_balance + user.activity_balance)
    }
```

#### 3. expire_activity_points()
```python
def expire_activity_points() -> dict:
    """
    处理过期的活动积分（定时任务调用）

    业务流程：
    1. 查询过期的 activity_claims（expire_at <= now, status='active'）
    2. 批量处理（每次100条）
    3. 扣除用户 activity_balance
    4. 更新 claim status = 'expired'
    5. 创建流水
    """
    from app.extensions import db

    expired_count = 0
    total_points_expired = Decimal('0.00')

    # 批量处理（避免长事务）
    BATCH_SIZE = 100

    while True:
        expired_claims = ActivityClaim.query.filter(
            ActivityClaim.expire_at <= datetime.now(ZoneInfo("Asia/Shanghai")),
            ActivityClaim.status == 'active'
        ).limit(BATCH_SIZE).with_for_update().all()

        if not expired_claims:
            break

        for claim in expired_claims:
            user = User.query.with_for_update().get(claim.user_id)
            if not user:
                continue

            # 计算实际扣除金额（不能超过当前余额）
            deduct_amount = min(claim.points_granted, user.activity_balance)

            if deduct_amount > 0:
                user.activity_balance -= deduct_amount

                # 创建流水
                transaction = Transaction(
                    user_id=claim.user_id,
                    type='activity_expire',
                    balance_type='activity',
                    amount=-deduct_amount,
                    balance_snapshot=user.activity_balance,
                    activity_id=claim.activity_id,
                    remark=f"Activity points expired"
                )
                db.session.add(transaction)

            # 更新状态
            claim.status = 'expired'
            claim.expired_at = datetime.now(ZoneInfo("Asia/Shanghai"))

            expired_count += 1
            total_points_expired += deduct_amount

        db.session.commit()

    return {
        "expired_count": expired_count,
        "total_points_expired": float(total_points_expired)
    }
```

#### 4. 其他辅助函数
```python
def get_checkin_status(user_id: int) -> dict:
    """获取签到状态"""

def get_active_activities() -> list:
    """获取当前可用活动列表"""

def get_user_activities(user_id: int, page: int, size: int) -> dict:
    """获取用户领取记录（分页）"""
```

### 3.5 新建 activity_admin_service.py - 管理员服务

**位置：** `F:\Mirage\backend\app\services\admin\activity_admin_service.py`

**核心功能：**
- create_activity(data)
- update_activity(activity_id, data)
- delete_activity(activity_id)
- get_activity_list(page, limit, status)
- get_activity_stats(activity_id)
- update_checkin_config(configs)
- get_checkin_config()

---

## 四、API 接口设计

### 4.1 用户端 API

**蓝图：** `F:\Mirage\backend\app\api\activities.py`
**前缀：** `/api/activities`

| 方法 | 路径 | 功能 | 认证 |
|------|------|------|------|
| POST | `/claim` | 领取活动积分 | JWT必须 |
| POST | `/checkin` | 每日签到 | JWT必须 |
| GET | `/checkin/status` | 获取签到状态 | JWT必须 |
| GET | `/my-claims` | 获取我的领取记录 | JWT必须 |
| GET | `/list` | 获取可用活动列表 | 公开 |

**示例：**

```json
// POST /api/activities/claim
Request: {"activity_code": "SPRING2024"}
Response: {
    "code": 200,
    "msg": "Activity claimed successfully",
    "data": {
        "points": 100,
        "expire_at": "2024-04-01T00:00:00",
        "current_activity_balance": 500,
        "total_balance": 1500
    }
}

// POST /api/activities/checkin
Response: {
    "code": 200,
    "msg": "Checkin successful",
    "data": {
        "points": 20,
        "consecutive_days": 3,
        "total_checkin_days": 15,
        "current_activity_balance": 520,
        "total_balance": 1520
    }
}
```

### 4.2 管理员端 API

**蓝图：** `F:\Mirage\backend\app\api\admin\activities.py`
**前缀：** `/api/admin`

| 方法 | 路径 | 功能 | 认证 |
|------|------|------|------|
| POST | `/activities` | 创建活动 | Admin必须 |
| PUT | `/activities/{id}` | 更新活动 | Admin必须 |
| DELETE | `/activities/{id}` | 删除活动 | Admin必须 |
| GET | `/activities` | 获取活动列表 | Admin必须 |
| GET | `/activities/{id}/stats` | 获取活动统计 | Admin必须 |
| GET | `/checkin/config` | 获取签到配置 | Admin必须 |
| PUT | `/checkin/config` | 更新签到配置 | Admin必须 |

**示例：**

```json
// POST /api/admin/activities
Request: {
    "code": "SPRING2024",
    "name": "春季福利",
    "description": "新春活动赠送积分",
    "points": 100,
    "expire_days": 30,
    "max_claims_per_user": 1,
    "required_level": 1,
    "start_at": "2024-03-01T00:00:00",
    "end_at": "2024-03-31T23:59:59"
}
Response: {
    "code": 0,
    "message": "Activity created successfully",
    "data": {"id": 1, ...}
}
```

### 4.3 修改现有 API

#### GET /api/users/me

**文件：** `app/api/users.py`

**新增返回字段：**
```json
{
    "code": 200,
    "data": {
        "id": 1,
        "email": "user@example.com",
        // 删除 balance 字段，改为详细信息
        "balance_detail": {
            "recharge_balance": 1000,
            "activity_balance": 500,
            "total_balance": 1500
        },
        "checkin_info": {
            "last_checkin_at": "2024-03-01T08:00:00",
            "total_checkin_days": 15
        }
    }
}
```

#### GET /api/wallet/balance

**文件：** `app/api/wallet.py`

**新增返回字段：**
```json
{
    "code": 200,
    "data": {
        "total_balance": 1500,
        "recharge_balance": 1000,
        "activity_balance": 500
    }
}
```

#### GET /api/wallet/transactions

**文件：** `app/api/wallet.py`

**流水记录新增字段：**
```json
{
    "id": 1,
    "type": "activity_grant",  // 新类型
    "balance_type": "activity",  // 新字段
    "activity_id": 1,  // 新字段
    "amount": 100,
    "balance_snapshot": 500,
    "created_at": "2024-03-01T08:00:00"
}
```

---

## 五、关键文件清单

### 5.1 需要修改的文件（10个）

| 文件 | 修改内容 |
|------|---------|
| `app/models/user.py` | 删除 balance，添加 recharge_balance, activity_balance, last_checkin_at, total_checkin_days |
| `app/models/wallet.py` | 修改 Transaction.type 枚举，添加 balance_type, activity_id 字段 |
| `app/services/pay_service.py` | 实现优先扣除逻辑，修改 check_and_deduct_balance, redeem_cdk, execute_refund |
| `app/services/task_service.py` | **删除扣费/退款代码**，改为调用 pay_service |
| `worker.py` | **删除退款代码**，改为调用 pay_service.execute_refund() |
| `app/api/users.py` | 修改 get_current_user() 返回积分详情 |
| `app/api/wallet.py` | 修改 get_balance() 和 get_transactions() 返回新字段 |
| `init_db.py` | 添加新表初始化、签到配置初始化 |
| `app/__init__.py` | 注册新蓝图 activities_bp, admin_activities_bp |
| `scheduler.py` | 添加定时任务 expire_activity_points |

### 5.2 需要新建的文件（6个）

| 文件 | 说明 |
|------|------|
| `app/models/activity.py` | Activity, ActivityClaim, CheckinConfig 模型 |
| `app/services/activity_service.py` | 活动核心业务逻辑 |
| `app/services/admin/activity_admin_service.py` | 管理员活动管理服务 |
| `app/api/activities.py` | 用户端活动 API 路由 |
| `app/api/admin/activities.py` | 管理员活动管理 API 路由 |
| `tests/test_services/test_activity_service.py` | 单元测试 |

---

## 六、实施步骤

### Phase 1: 数据模型修改

1. **修改 `app/models/user.py`**
   - 删除 balance 字段
   - 添加 recharge_balance, activity_balance, last_checkin_at, total_checkin_days
   - 修改 to_dict() 方法

2. **修改 `app/models/wallet.py`**
   - 扩展 Transaction.type 枚举
   - 添加 balance_type, activity_id 字段
   - 修改 to_dict() 方法

3. **创建 `app/models/activity.py`**
   - Activity 模型
   - ActivityClaim 模型
   - CheckinConfig 模型

4. **修改 `init_db.py`**
   - 添加新表初始化
   - 添加签到配置初始化

5. **运行初始化**
   ```bash
   python init_db.py
   ```

### Phase 2: 业务逻辑重构

1. **修改 `app/services/pay_service.py`**
   - 实现优先扣除逻辑（check_and_deduct_balance）
   - 修改 CDK 兑换（redeem_cdk）
   - 修改退款（execute_refund）

2. **修改 `app/services/task_service.py`**
   - 删除 line 97-98, 115 的扣费代码
   - 删除 line 205-206 的退款代码
   - 导入并调用 pay_service 方法

3. **修改 `worker.py`**
   - 删除 line 363-379 的退款 SQL 代码
   - 改为调用 pay_service.execute_refund()
   - 添加 db.session.remove() 清理 session

4. **创建 `app/services/activity_service.py`**
   - 实现活动领取、签到、过期处理等逻辑

5. **创建 `app/services/admin/activity_admin_service.py`**
   - 实现管理员活动管理逻辑

### Phase 3: API 开发

1. **创建 `app/api/activities.py`** - 用户端 API
2. **创建 `app/api/admin/activities.py`** - 管理员 API
3. **修改 `app/api/users.py`** - 扩展用户信息
4. **修改 `app/api/wallet.py`** - 扩展余额信息
5. **修改 `app/__init__.py`** - 注册新蓝图

### Phase 4: 定时任务

1. **修改 `scheduler.py`**
   - 添加 expire_activity_points 定时任务（每小时执行）

### Phase 5: 测试

1. **创建单元测试**
2. **手动集成测试**（见第七节）

---

## 七、验证方案

### 7.1 单元测试

创建 `tests/test_services/test_activity_service.py`：

```python
def test_claim_activity_success()  # 成功领取活动
def test_claim_activity_duplicate()  # 重复领取失败
def test_daily_checkin_success()  # 成功签到
def test_daily_checkin_duplicate()  # 重复签到失败
def test_consecutive_checkin()  # 连续签到
def test_deduct_balance_priority()  # 优先扣除活动积分
def test_expire_activity_points()  # 过期积分扣除
```

### 7.2 集成测试流程

1. **初始化数据库**
   ```bash
   python init_db.py
   ```

2. **创建测试活动（管理员）**
   ```bash
   POST /api/admin/activities
   {"code": "TEST2024", "points": 100, "expire_days": 7, ...}
   ```

3. **用户签到**
   ```bash
   POST /api/activities/checkin
   # 验证 activity_balance 增加
   ```

4. **用户领取活动**
   ```bash
   POST /api/activities/claim
   {"activity_code": "TEST2024"}
   # 验证 activity_balance 增加
   ```

5. **提交任务（验证优先扣除）**
   - 设置用户：recharge_balance=100, activity_balance=50
   - 提交任务，成本30
   - 验证：activity_balance=20, recharge_balance=100
   - 再提交任务，成本40
   - 验证：activity_balance=0, recharge_balance=80

6. **查看流水记录**
   ```bash
   GET /api/wallet/transactions
   # 验证 balance_type 字段正确
   ```

7. **运行过期任务**
   ```bash
   python -c "from app.services.activity_service import expire_activity_points; expire_activity_points()"
   # 修改数据库中某条 activity_claims.expire_at 为过去时间
   # 验证积分被扣除，status 变为 expired
   ```

### 7.3 边界情况测试

- 活动积分不足扣除全额
- 活动积分过期时余额不足
- 跨天签到判断
- 连续签到断档

---

## 八、输出文档（给前端开发）

### 8.1 新增 API 接口（12个）

**用户端（5个）：**
- POST /api/activities/claim - 领取活动积分
- POST /api/activities/checkin - 每日签到
- GET /api/activities/checkin/status - 获取签到状态
- GET /api/activities/my-claims - 获取我的领取记录
- GET /api/activities/list - 获取可用活动列表

**管理员端（7个）：**
- POST /api/admin/activities - 创建活动
- PUT /api/admin/activities/{id} - 更新活动
- DELETE /api/admin/activities/{id} - 删除活动
- GET /api/admin/activities - 获取活动列表
- GET /api/admin/activities/{id}/stats - 获取活动统计
- GET /api/admin/checkin/config - 获取签到配置
- PUT /api/admin/checkin/config - 更新签到配置

### 8.2 修改的 API 接口（3个）

**GET /api/users/me**
- 删除 balance 字段
- 新增 balance_detail: {recharge_balance, activity_balance, total_balance}
- 新增 checkin_info: {last_checkin_at, total_checkin_days}

**GET /api/wallet/balance**
- 新增 recharge_balance, activity_balance 字段

**GET /api/wallet/transactions**
- 流水记录新增：balance_type, activity_id 字段
- type 新增枚举值：checkin, activity_grant, activity_expire

### 8.3 完整 API 文档

详细接口文档将在实现完成后输出，包括：
- 请求参数详细说明
- 响应数据格式
- 错误码说明
- 完整调用示例

---

## 九、技术要点

### 9.1 并发安全
- 使用悲观锁（`with_for_update()`）处理余额变动
- 活动领取、签到、扣费均在事务中执行

### 9.2 数据一致性
- 所有积分变动都记录流水（transactions 表）
- balance_snapshot 记录变动后余额快照
- 支持审计和回溯

### 9.3 性能优化
- 签到配置使用 Redis 缓存（1小时TTL）
- 活动查询使用索引（status, start_at, end_at）
- 过期积分批量处理（每次100条）

### 9.4 Session 管理
- worker.py 调用 pay_service 后执行 `db.session.remove()` 清理 session
- 避免长时间运行的 worker 出现 session 缓存问题

---

## 十、风险与注意事项

1. **代码重构风险**
   - task_service.py 和 worker.py 的修改涉及核心扣费逻辑
   - 需要充分测试，确保不影响现有功能

2. **Session 管理**
   - worker.py 调用 ORM 后需要清理 session
   - 建议增加单元测试验证

3. **定时任务可靠性**
   - 过期积分处理依赖定时任务
   - 建议添加监控和告警

4. **并发性能**
   - 悲观锁可能影响高并发场景
   - 如需优化，可考虑乐观锁 + 重试

5. **时区问题**
   - 统一使用 UTC 时间
   - 前端根据用户时区转换

---

## 总结

本计划包含：
- **删除旧字段**：balance 字段完全移除
- **数据库变更**：修改2个表，新增3个表
- **代码重构**：统一扣费/退款逻辑，删除 task_service.py 和 worker.py 中的重复代码
- **新增功能**：活动积分、签到、优先扣除逻辑
- **API 接口**：新增12个，修改3个

所有设计基于现有架构，保持一致的编码风格。实施后将为平台提供完整的活动运营能力。
